<UserControl x:Class="SmartVocabulary.UI.SettingsPages.RowAlternationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xceed="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d">
    <Grid>
        <TextBlock TextWrapping="Wrap"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Text="Row Alternation Color" />
        <xceed:ColorPicker Margin="0,20,0,0"
                           VerticalAlignment="Top"
                           SelectedColor="{Binding SelectedAlternationColor, UpdateSourceTrigger=PropertyChanged}"
                           Height="23" 
                           AvailableColorsHeader="{Binding SelectedAlternationColor, UpdateSourceTrigger=PropertyChanged}">
            
        </xceed:ColorPicker>
    </Grid>
</UserControl>
